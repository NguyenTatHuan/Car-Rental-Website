apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: car-postgres-data
  labels:
    app: car-postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 100Mi
