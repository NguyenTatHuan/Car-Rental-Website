apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: feedback-postgres-data
  labels:
    app: feedback-postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: standard
