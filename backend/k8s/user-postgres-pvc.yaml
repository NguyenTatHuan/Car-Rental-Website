apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-postgres-data
  labels:
    io.kompose.service: user-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
